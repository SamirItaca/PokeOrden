# Dependecias generales y por tecnologias
typescript + ts-node → para TS
nodemon → recarga automática en dev

    - Frontend
        cors → habilitar requests desde frontend

    - Backend
        express → servidor
        dotenv → variables de entorno

    - Database
        prisma y @prisma/client → ORM